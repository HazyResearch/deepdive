person (
    person_id bigint,
    name text
    ).

person_has_cancer (
    person_id bigint,
    has_cancer boolean,
    id bigint
).

person_smokes (
    person_id bigint,
    smokes boolean,
    id bigint
).

friends (
    person_id bigint,
    friend_id bigint
).
