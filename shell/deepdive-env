#!/usr/bin/env bash
# deepdive-env -- run commands inside deepdive's environment
# > deepdive env [COMMAND [ARG]...]
##
# Author: Jaeho Shin <netj@cs.stanford.edu>
# Created: 2012-05-10
set -eu

[ $# -gt 0 ] || set -- bash --norc -i

exec "$@"
