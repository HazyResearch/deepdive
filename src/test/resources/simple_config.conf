deepdive {
  
  global.connection: {
    host: "localhost"
    port: 5432
    db: "deepdive_test"
    user: "root"
    password: "password"
  }

  relations.documents.schema: {
    id: Integer,
    text: Text,
    meta: Text
  }

  relations.entities.schema: {
    id: Integer,
    document_id: Integer,
    name: String,
    meta: Text
  }

  ingest: {
    documents.source: "data/documents.tsv"
  }

  extractions: {

    extractor1.output_relation: "entities"
    extractor1.input: "SELECT * FROM documents"
    extractor1.udf: "udf/entities.py"
    extractor1.factor.name: "Entities"
    extractor1.factor.function: "id = Imply()"
    extractor1.factor.weight: "?"
  
  }

}